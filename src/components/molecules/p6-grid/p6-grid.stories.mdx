import { Story, Preview, Meta } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import readme from "./readme.md";

<Meta
  title="Molecules/Grid"
  component="p6-grid"
  parameters={{ notes: readme }}
/>

# Grid

`p6-grid` provides a grid component with templating.

<Preview>
  <Story name="Default" height="300px">
    {() => {
      const headers = [
        {color: "blue", id: "col-1", label: "First", width: 150},
        {id: "col-2", label: "Second", width: 100},
        {id: "col-3", label: "Third", width: 100},
        {id: "col-4", label: "Fourth", width: 100},
      ];
      const rows = [
        [
          {headerId: "col-2", label: "second cell"},
          {align: "center", color: "blue", headerId: "col-1", label: "first cell", width: 150},
          {headerId: "col-3", label: "third cell"},
          {headerId: "col-4", label: "fourth cell"},
        ],
        [
          {headerId: "col-2", label: "second cell"},
          {headerId: "col-3", label: "third cell"},
          {align: "center", color: "blue", headerId: "col-1", label: "first cell", width: 150},
          {headerId: "col-4", label: "fourth cell"},
        ],
        [
          {headerId: "col-2", label: "second cell"},
          {headerId: "col-3", label: "third cell"},
          {headerId: "col-4", label: "fourth cell"},
          {align: "center", color: "blue", headerId: "col-1", label: "first cell", width: 150},
        ],
      ];
      const grid = document.createElement("p6-grid");
      const body = document.createElement("p6-grid-body");
      grid.id = "default-grid";
      grid.headers = headers;
      grid.rows = rows;
      return grid;
    }}
  </Story>
</Preview>

import { Story, Preview, Meta } from "@storybook/addon-docs/blocks";

import readme from "./readme.md";

<Meta
  title="Templates/Waiting"
  component="p6-waiting"
  parameters={{ notes: readme }}
/>

# Waiting

`p6-waiting` provides a spinner

<Preview>
  <Story name="Default">
    {() => {
      return `<p6-waiting></p6-waiting>`;
    }}
  </Story>
</Preview>

<Story name="Centered" height="300px" parameters={{ docs: { disable: true } }}>
  {() => {
    return '<p6-waiting style="height:280px;"></p6-waiting>';
  }}
</Story>

<Story
  name="With message"
  height="300px"
  parameters={{ docs: { disable: true } }}
>
  {() => {
    return '<p6-waiting style="height:280px;">Loading data...</p6-waiting>';
  }}
</Story>

<Story
  name="With rich message"
  height="300px"
  parameters={{ docs: { disable: true } }}
>
  {() => {
    return '<p6-waiting style="height:280px;">Loading data... <p6-link>Cancel</p6-link></p6-waiting>';
  }}
</Story>

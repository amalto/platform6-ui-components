import {
  Story,
  Preview,
  Meta,
  Markdown,
  Description,
} from "@storybook/addon-docs/blocks";

import Readme from "./readme.md";

import {
  DefaultStory,
  TextStory,
  SizeStory,
  Previews,
} from "./p6-input.stories.ts";

<Meta
  title="Atoms/Input"
  component="p6-input"
  parameters={{
    actions: {
      handles: ["p6Submit p6-form"],
    },
  }}
/>

# Input

<Preview>
  <Story name="Default" height="58px" parameters={Previews.Default}>
    {DefaultStory()}
  </Story>
</Preview>

<Preview>
  <Story name="Text" height="58px" parameters={Previews.Text()}>
    {TextStory()}
  </Story>
</Preview>

<Preview>
  <Story
    name="Read only"
    height="58px"
    parameters={Previews.Text({ readonly: true })}
  >
    {TextStory({ readonly: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Disabled"
    height="58px"
    parameters={Previews.Text({ disabled: true })}
  >
    {TextStory({ disabled: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Waiting"
    height="58px"
    parameters={Previews.Text({ waiting: true })}
  >
    {TextStory({ waiting: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="On error"
    height="58px"
    parameters={Previews.Text({ pattern: "42", value: "84" })}
  >
    {TextStory({ pattern: "42", value: "84" })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Size"
    height="58px"
    parameters={Previews.Text({ size: "small" })}
  >
    {SizeStory({ size: "small" })}
  </Story>
</Preview>

<Description markdown={Readme} />

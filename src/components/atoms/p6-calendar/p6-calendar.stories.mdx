import { Story, Preview, Meta } from "@storybook/addon-docs/blocks";
import {
  boolean,
  select,
  number,
  text as kText,
  withKnobs,
} from "@storybook/addon-knobs";
import { action, decorate } from "@storybook/addon-actions";
import readme from "./readme.md";

<Meta
  title="Atoms/Calendar"
  component="p6-calendar"
  parameters={{ notes: readme }}
/>

# Calendar

<Story
  name="Default"
  parameters={{ docs: { disable: true } }}
  decorators={[withKnobs]}
>
  {() => {
    const kRequired = boolean("Required", false);
    const kReadOnly = boolean("Read only", false);
    const kDisabled = boolean("Disabled", true);
    const kColor = select(
      "Color",
      ["default", "primary", "info", "success", "warning", "danger"],
      "danger"
    );
    const kType = select("Type", ["date", "datetime", "time"], "date");
    const kLang = select("Language", ["en", "fr", "ru"], "en");
    const kIsRange = boolean("Is range", true);
    const kStartDate = kText("Start date", "");
    const kEndDate = kText("End date", "");
    const kMinDate = kText("Min date", "");
    const kMaxDate = kText("Max date", "");
    const kMinuteSteps = number("Minute step", 5);
    const kLabelFrom = kText("Label from", "");
    const kLabelTo = kText("Label to", "");
    return `
      <p6-calendar
        name="date"
        lang="${kLang}"
        type="${kType}"
        color="${kColor}"
        readOnly=${kReadOnly}
        required=${kRequired}
        disabled=${kDisabled}
        isRange=${kIsRange}
        startDate="${kStartDate}"
        endDate="${kEndDate}"
        minDate="${kMinDate}"
        maxDate="${kMaxDate}"
        minuteSteps=${kMinuteSteps}
        labelFrom="${kLabelFrom}"
        labelTo="${kLabelTo}"/>
    `;
  }}
</Story>

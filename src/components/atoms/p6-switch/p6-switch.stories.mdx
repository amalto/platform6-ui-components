import {
  Story,
  Preview,
  Meta,
  Markdown,
  Description,
} from "@storybook/addon-docs/blocks";

import Readme from "./readme.md";

import {
  DefaultStory,
  SelectedStory,
  EnabledStory,
  DisabledStory,
  SizeStory,
  ModeStory,
  FormStory,
} from "./p6-switch.stories.ts";

<Meta
  title="Atoms/Switch"
  component="p6-switch"
  parameters={{
    actions: {
      handles: ["p6Submit p6-form"],
    },
  }}
/>

# Switch

`p6-switch` provide a switch component which allow you to _enable_ or _disable_ a field.

<Preview>
  <Story
    name="Default"
    height="84px"
    parameters={DefaultStory.parameters}
    args={DefaultStory.args}
    argTypes={DefaultStory.argTypes}
  >
    {DefaultStory}
  </Story>
</Preview>

### Enabled

Basic implementation enabled by default.

<Preview>
  <Story
    name="Enabled"
    height="84px"
    parameters={EnabledStory.parameters}
    args={EnabledStory.args}
    argTypes={EnabledStory.argTypes}
  >
    {EnabledStory}
  </Story>
</Preview>

### Disabled

The `disabled` _prop_ will keep you from editing it. In the case you are using it inside a form, it won't populate the submit object.

<Preview>
  <Story
    name="Disabled"
    height="84px"
    parameters={DisabledStory.parameters}
    args={DisabledStory.args}
    argTypes={DisabledStory.argTypes}
  >
    {DisabledStory}
  </Story>
</Preview>

### Mode

<Preview>
  <Story
    name="Mode"
    height="84px"
    parameters={ModeStory.parameters}
    args={ModeStory.args}
    argTypes={ModeStory.argTypes}
  >
    {ModeStory}
  </Story>
</Preview>

### Size

<Preview>
  <Story
    name="Size"
    height="84px"
    parameters={SizeStory.parameters}
    args={SizeStory.args}
    argTypes={SizeStory.argTypes}
  >
    {SizeStory}
  </Story>
</Preview>

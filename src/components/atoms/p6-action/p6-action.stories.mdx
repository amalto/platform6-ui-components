import { library } from "@fortawesome/fontawesome-svg-core";
import { fab } from "@fortawesome/free-brands-svg-icons";
import { fas } from "@fortawesome/free-solid-svg-icons";
import { action } from "@storybook/addon-actions";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { select, text, boolean, withKnobs } from "@storybook/addon-knobs";
import readme from "./readme.md";

<Meta
  title="Atoms/Action"
  component="p6-action"
  parameters={{ notes: readme }}
/>

# Action

<Story
  name="Default"
  parameters={{ docs: { disable: true } }}
  decorators={[withKnobs]}
>
  {(el) => {
    const modes = [
      "danger",
      "warning",
      "default",
      "info",
      "success",
      "primary",
    ];
    const sizes = ["small", "default", "normal", "medium", "large"];
    const kModes = select("Modes", modes, "default");
    const kSize = select("Size", sizes, "default");
    const kWaiting = boolean("Waiting", false);
    const kDisabled = boolean("Disabled", false);
    const kTransform = text("Transformation");
    const component = document.createElement("p6-action");
    component.mode = kModes;
    component.waiting = kWaiting;
    component.disabled = kDisabled;
    component.size = kSize;
    component.addEventListener("click", action("Click"));
    component.innerHTML = `<p6-icon name="home" transform=${kTransform} />`;
    return component;
  }}
</Story>

<Preview>
  <Story name="Size" height="63px">
    {`
      <p6-action size="small"><p6-icon name="home" /></p6-action>
      <p6-action><p6-icon name="home" /></p6-action>
      <p6-action size="normal"><p6-icon name="home" /></p6-action>
      <p6-action size="medium"><p6-icon name="home" /></p6-action>
      <p6-action size="large"><p6-icon name="home" /></p6-action>
    `}
  </Story>
</Preview>

<Preview>
  <Story name="Disabled" height="42px">
    {`
      <p6-action disabled><p6-icon name="home" /></p6-action>
    `}
  </Story>
</Preview>

<Preview>
  <Story name="Waiting" height="42px">
    {`
      <p6-action waiting><p6-icon name="home" /></p6-action>
    `}
  </Story>
</Preview>

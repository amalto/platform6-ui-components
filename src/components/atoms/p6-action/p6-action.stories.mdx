import {
  Story,
  Preview,
  Meta,
  Markdown,
  Description,
} from "@storybook/addon-docs/blocks";

import Readme from "./readme.md";

import {
  DefaultStory,
  SizeStory,
  ModeStory,
  WaitingStory,
  DisabledStory,
} from "./p6-action.stories.ts";

<Meta
  title="Atoms/Action"
  component="p6-action"
  parameters={{
    actions: {
      handles: ["p6Submit p6-form"],
    },
  }}
/>

# Action

Let's define a p6-action component.

<Preview>
  <Story
    name="Default"
    height="58px"
    parameters={DefaultStory.parameters}
    args={DefaultStory.args}
    argTypes={DefaultStory.argTypes}
  >
    {DefaultStory}
  </Story>
</Preview>

we can change the size

<Preview>
  <Story
    name="Sizes"
    height="58px"
    parameters={SizeStory.parameters}
    args={SizeStory.args}
    argTypes={SizeStory.argTypes}
  >
    {SizeStory}
  </Story>
</Preview>

or even the mode

<Preview>
  <Story
    name="Modes"
    height="58px"
    parameters={ModeStory.parameters}
    args={ModeStory.args}
    argTypes={ModeStory.argTypes}
  >
    {ModeStory}
  </Story>
</Preview>

it could have a waiting attribute

<Preview>
  <Story
    name="Waiting"
    height="58px"
    parameters={WaitingStory.parameters}
    args={WaitingStory.args}
    argTypes={WaitingStory.argTypes}
  >
    {WaitingStory()}
  </Story>
</Preview>

or be disabled

<Preview>
  <Story
    name="Disabled"
    height="58px"
    parameters={DisabledStory.parameters}
    args={DisabledStory.args}
    argTypes={DisabledStory.argTypes}
  >
    {DisabledStory()}
  </Story>
</Preview>

<Description markdown={Readme} />

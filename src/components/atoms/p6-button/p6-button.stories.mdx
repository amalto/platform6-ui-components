import {
  Story,
  Preview,
  Meta,
  Markdown,
  Description,
} from "@storybook/addon-docs/blocks";

import Readme from "./readme.md";

import {
  DefaultStory,
  SizeStory,
  ModeStory,
  IconStory,
} from "./p6-button.stories.ts";

<Meta
  title="Atoms/Button"
  component="p6-button"
  parameters={{
    actions: {
      handles: ["p6Submit p6-form"],
    },
  }}
/>

# Button

<Preview>
  <Story name="Default" height="58px" parameters={DefaultStory.parameters}>
    {DefaultStory()}
  </Story>
</Preview>

<Preview>
  <Story name="Sizes" height="58px" parameters={SizeStory.parameters}>
    {SizeStory()}
  </Story>
</Preview>

<Preview>
  <Story name="Modes" height="58px" parameters={ModeStory.parameters()}>
    {ModeStory()}
  </Story>
</Preview>

<Preview>
  <Story
    name="Waiting"
    height="58px"
    parameters={ModeStory.parameters({ waiting: true })}
  >
    {ModeStory({ waiting: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Disabled"
    height="58px"
    parameters={ModeStory.parameters({ disabled: true })}
  >
    {ModeStory({ disabled: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Outlined"
    height="58px"
    parameters={ModeStory.parameters({ outlined: true })}
  >
    {ModeStory({ outlined: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Outlined and Waiting"
    height="58px"
    parameters={ModeStory.parameters({ outlined: true, waiting: true })}
  >
    {ModeStory({ outlined: true, waiting: true })}
  </Story>
</Preview>

<Preview>
  <Story
    name="Outlined and Disabled"
    height="58px"
    parameters={ModeStory.parameters({ outlined: true, disabled: true })}
  >
    {ModeStory({ outlined: true, disabled: true })}
  </Story>
</Preview>

<Preview>
  <Story name="With Icon" height="58px" parameters={IconStory.parameters}>
    {IconStory()}
  </Story>
</Preview>

<Description markdown={Readme} />
